<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NumberProgressBar_Blue">
        <item name="progress_max">100</item>
        <item name="progress_current">0</item>
        <item name="progress_unreached_color">@color/grey_f0</item>
        <item name="progress_reached_color">@color/colorPrimary</item>
        <item name="progress_text_size">0sp</item>
        <item name="progress_reached_bar_height">1.7dp</item>
        <item name="progress_unreached_bar_height">1.7dp</item>
    </style>
</resources>
